import { IconButton } from '@ufoui/core';

import { ButtonShowcasePage } from '../components/shared/buttonShowcasePage';

export const IconButtonPage = () => {
  return <ButtonShowcasePage component={IconButton} title="IconButton" />;
};
