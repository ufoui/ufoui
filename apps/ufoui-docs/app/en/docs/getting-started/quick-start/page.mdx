# Quick Start

Wrap your app with the ufoui theme provider and render your first component.

```tsx
import { Button, ThemeProvider } from '@ufoui/core';

export function App() {
    return (
        <ThemeProvider>
            <Button filled label="Get started" />
        </ThemeProvider>
    );
}
```
